rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow authenticated users to access CRM data
    match /crm/{document} {
      allow read, write: if request.auth != null;
    }
  }
}
